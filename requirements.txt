# --- nucleo ---
numpy>=1.26
pandas>=2.2
scipy>=1.11
pyarrow>=15.0          # cache Parquet
PyYAML>=6.0
joblib>=1.3

# --- machine learning ---
scikit-learn>=1.4

# --- optimizacion ---
optuna>=3.5            # refinamiento bayesiano
# DEAP estaba aqui y se ha quitado a proposito: el motor genetico de
# goldbot/evolution esta escrito a mano (el genoma es un arbol de reglas
# plano, no un arbol GP arbitrario) y DEAP no se importa en ninguna parte.
# Al ser una extension en C, cada version nueva de Python la deja sin wheel
# durante meses y rompia la instalacion sin aportar nada.

# --- datos y ejecucion ---
yfinance>=0.2.40       # futuros/spot de oro (GC=F, XAUUSD=X)
ccxt>=4.2              # 100+ exchanges; PAXG/USDT como proxy de oro
requests>=2.31

# --- pruebas ---
pytest>=8.0

# =====================================================================
# OPCIONALES (instalar solo si se necesitan)
# =====================================================================
# Redes neuronales (ml.model_type: torch_mlp).
# El boosting de gradiente suele rendir igual o mejor en datos tabulares
# de este tamano, asi que torch no es necesario para operar.
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# Ejecucion real de XAU/USD via MetaTrader 5 (SOLO WINDOWS):
#   pip install MetaTrader5
#
# Backtesting cruzado con otros motores (verificacion independiente):
#   pip install backtrader vectorbt
